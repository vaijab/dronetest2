---
pipeline:
  build:
    image: "docker:17.07.0"
    environment:
      - "DOCKER_HOST=tcp://${HOSTNAME}:2375"
    commands:
      - env
      - docker info

# services:
#   dind:
#     image: "docker:17.07.0-dind"
#     privileged: true
#     command:
#       - "-s"
#       - "overlay"
